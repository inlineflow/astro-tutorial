---
interface Props {
  tags: string[];
}
import "../styles/global.css";
const { tags } = Astro.props;
---

<div class="flex flex-row gap-2 text-sm mt-2 mb-2">
  {tags.map((tag) => <p class="bg-orange-300 p-2.5 rounded-md">{tag}</p>)}
</div>
